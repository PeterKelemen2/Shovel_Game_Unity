using UnityEngine;
using System.Collections;
using UnityEngine.UI;
public class FadeToColor : MonoBehaviour
{

    public GameObject parent;

    public void fadeToColor(Color desiredColor, float fadeTime)
    {
        //parent.CrossFadeColor(desiredColor, fadeTime, true, true);
    }
}
